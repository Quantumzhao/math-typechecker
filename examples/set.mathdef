> A := finite Set
A is finite Set A

> B := Set
B is Set B

> C := Natural
C is Natural

> powerSet := AnySet -> Set of s where s is subset of AnySet

> PB := powerSet B
PB is Set type of s where
    s isSubsetOf B

> moreinfo PB
PB is Set typeof s where
    s is subset of B where
        B is Set B

> f := PB -> B
f is Set PB -> Set B

> moreinfo f
f is Set PB -> Set B where
    PB is Set type of s where
        s is subset of B 
    B is Set B

> AB := A intersect B
AB is finite Set AB where 
    AB is related to B by subset
    AB is related to A by subset

> moreinfo AB
AB is finite set AB where
    AB is related to B by subset
    AB is related to A by subset

> g := A -> B
g is finite Set A -> Set B

> g(A intersect B)
finite Set B' where
    B' is subset of B

> ab := element in A intersect B
ab is element in A intersect B

> g(ab)
g(ab) is element in B

> A cross B
Set typeof (a, b) where
    a is in A
    b is in B

> ArB := A ~ B
A isrelatedto B by ArB

> A isrelatedto B
Invalid Input

> f := A -> B -> C
f is finite Set A -> Set B -> Set C

> f(ab)
Set B -> Set C

> f(ab, B)
Set C

> f(ab, ab)
element in C

> g := D -> A
Error: D is not defined

> exit

> Ar(X) := A ~ X by ArB, ~ is proper
Ar is a 

> pSet := A -> B where 
    B is Set typeof s where 
        s is subset of A
pSet is A -> B where 
    B is Set typeof s where 
        s is subset of A

> pSet(D) where D is subset of A
Set typeof s where 
    s is subset of D

> (a, b) where 
    a is element in A
    b is element in B
(a, b) is element in A cross B

> h := A -> B, -> is injective structural
