Empty := finite Set

intersect := A' * B' -> C' where
    A' := AnySet
    B' := AnySet
    C' := AnySet
    assume A' ~ C' by isSubsetOf
    assume B' ~ C' by isSubsetOf

union := Set A * Set B -> Set C where
    A' := AnySet
    B' := AnySet
    C' := AnySet
    assume C' ~ A' by isSubsetOf
    assume C' ~ B' by isSubsetOf

Natural := finite Set

Real := Set

assume Natural ~ Real by isSubsetOf

add := Real * Real -> Real
